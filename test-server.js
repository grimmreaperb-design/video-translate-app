const express = require("express"); const app = express(); const PORT = 3001; app.use(express.json()); app.get("/health", (req, res) => { res.json({ status: "OK", message: "Backend is running!" }); }); app.get("/", (req, res) => { res.json({ message: "Video Translate App Backend" }); }); app.listen(PORT, () => { console.log(`🚀 Server running on port ${PORT}`); console.log(`📊 Health check: http://localhost:${PORT}/health`); console.log(`🌐 Main endpoint: http://localhost:${PORT}/`); });
